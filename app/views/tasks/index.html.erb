<h2><%= t("tasks.task_list") %></h2>
<%= link_to "新增待辦事項", new_task_path %>
<table>
  <thead>
    <tr>
      <td><%= t("tasks.task_title") %></td>
      <td><%= t("tasks.task_detail") %></td>
      <td></td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |t| %>
    <tr>
      <td><%= link_to(t.title, task_path(t)) %></td>
      <td><%= t.description %></td>
      <td><%= link_to "edit", edit_task_path(t) %></td>
      <td><%= link_to "delete", task_path(t), method: "delete", data:{ confirm: "確認刪除" } %></td>
    </tr>
    <% end %>
  </tbody>
</table>
